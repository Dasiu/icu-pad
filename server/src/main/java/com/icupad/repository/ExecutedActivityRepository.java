package com.icupad.repository;

import com.icupad.domain.nursing_process.ExecutedActivity;
import org.springframework.stereotype.Repository;

@Repository
public interface ExecutedActivityRepository extends BaseRepository<ExecutedActivity, Long> {
}
